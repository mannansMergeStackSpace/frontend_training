import { makeStyles } from "@mui/styles";

const InfoStyles = makeStyles({
  parentContainer: {
    display: "flex",
    marginTop: "110px",
  },
  container: {
    width: "63%",
  },
});

export default InfoStyles;

import { makeStyles } from "@mui/styles";

const ContainerMainStyles = makeStyles({
  container: {
    borderRadius: "20px",
    background: "#FFFFFF",
    boxShadow: "0px 18px 32px 0px info.dark",
    marginTop: "50px",
    padding: "30px",
  },
  headerContainer: {
    display: "block",
    marginBottom: "10px",
    textAlign: "start",
  },
});

export default ContainerMainStyles;
